Review the following files and make sure that all of the important functions, classes and methods have JSDoc documentation explaining what they do and their purpose so that we can understand them later.

REMEBER NOT everything should have jsdoc, just important functions/classes/methods, especially which have params and important logic.

!'git diff --name-only -- '*.ts' '*.tsx' '*.js' '*.cjs' '*.jsx''